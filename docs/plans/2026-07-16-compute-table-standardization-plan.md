# Compute Workbench Table Standardization Plan

1. Add and test the virtualization domain table wrapper.
2. Replace duplicated wrappers in VM, cluster, image, flavor, and operation pages.
3. Pass query refresh state into route-level virtualization tables.
4. Align the compute access table toolbar and cursor summary.
5. Audit Docker route and detail tables for fixed action, overflow, controls, and pagination.
6. Run repository checks, update Graphify, and verify representative pages in the browser.
